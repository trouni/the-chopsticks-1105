<%= simple_form_for [restaurant, review], data: { action: 'submit->insert-in-list#submit' } do |f| %>
  <%= f.input :content, label: false %>
  <%= f.submit class: 'btn btn-primary mt-2' %>
<% end %>
