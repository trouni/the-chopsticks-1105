<div class="container mt-3">
  <h1>Add Review</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.submit class: 'btn btn-primary mt-2' %>
  <% end %>
</div>
<%# sf + tab -> builds regular simple_form_for  %>
<%# sfn + tab -> builds nested simple_form_for  %>
